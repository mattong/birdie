.jumbotron
  h2 WELCOME TO BIRDIE
   p= @user.name

  a(href=session_path(@conn, :delete))= gettext "Sign out!"
.container
  .col-md-6.col-offset-3
    =render "form.html", conn: @conn, changeset: @changeset, action: chirp_path(@conn, :chirp)
ul
= for chirp <- @chirps do
  li= "#{chirp.content} | @#{chirp.user.handle}"

a(href=profile_path(@conn, :index, @user.handle))= gettext "Profile mo 'to"
